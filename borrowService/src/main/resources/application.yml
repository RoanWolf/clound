server:
  port: 8081

spring:
  application:
    name: borrowService
  datasource:
    url: jdbc:mysql://localhost:3306/bm?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
